import { describe, expect, it } from "vitest";
import { StyleStep } from "./StyleStep";

describe("StyleStep", () => {
  it("exports a component", () => {
    expect(typeof StyleStep).toBe("function");
  });
});
