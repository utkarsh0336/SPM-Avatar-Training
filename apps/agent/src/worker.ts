/**
 * LiveKit agent worker — Mode B (photoreal/enterprise avatar mode) lands in
 * Phase 6. Phase 0 just needs a real, buildable stub.
 */
export async function runWorker(): Promise<string> {
  return "agent worker stub — LiveKit wiring lands in Phase 6";
}
