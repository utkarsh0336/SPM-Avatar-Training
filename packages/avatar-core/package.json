{
  "name": "@avatrain/avatar-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "bench:latency": "node -e \"console.log('no benchmarks yet — needs a live LiveKit deployment')\""
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.5",
    "simli-client": "^3.0.2",
    "three": "^0.185.1"
  }
}
