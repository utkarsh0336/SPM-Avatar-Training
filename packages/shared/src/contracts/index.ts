/**
 * Public contracts consumed by customer code we cannot redeploy. Additive
 * changes only — breaking changes need a new CDN major and a migration
 * note. See .claude/rules/embed.md.
 */
export {};
